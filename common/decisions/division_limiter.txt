decrease_division_amount = {
    decrease_military = {
        visible = {
            is_ai = yes
        }
        available = {
            is_ai = yes
        }
        activation = {
            check_variable = { num_divisions > division_limit }
        }
        is_good = no
        selectable_mission = no
        days_mission_timeout = 3
        timeout_effect = {
            country_event = { id = division_limit_event.1 days = 0 }
            if = {
                limit = {
                    check_variable = { num_divisions > division_limit }
                }
                activate_mission = decrease_military_ten_percent
            }
        }
    }
}