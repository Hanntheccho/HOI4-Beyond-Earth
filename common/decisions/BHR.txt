BHR_preparing_for_war = {
	BHR_initiate_remilitarization = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
			NOT = {
				has_global_flag = india_was_reunited_flag
			}
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
	
		cost = 60
		days_remove = 10
	
		remove_effect = {
			country_event = { id = bharat_event.1 days = 0 }
			custom_effect_tooltip = BHR_faster_conversion_between_civs_and_mils
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	BHR_foster_industrial_growth = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
			NOT = {
				has_global_flag = india_was_reunited_flag
			}
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
	
		cost = 60
		days_remove = 10
	
		remove_effect = {
			country_event = { id = bharat_event.2 days = 0 }
			custom_effect_tooltip = BHR_faster_development_of_industry_and_infra
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	BHR_rebuild_lucknow = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
		
		custom_cost_trigger = {
			check_variable = { industrial_power > 40 }
		}
		custom_cost_text = decision_cost_40_ip
		days_remove = 50
	
		remove_effect = {
			country_event = { id = bharat_event.3 days = 0 }
			custom_effect_tooltip = BHR_rebuild_ruined_city
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	BHR_reinforce_the_cities = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
		
		custom_cost_trigger = {
			check_variable = { industrial_power > 40 }
		}
		custom_cost_text = decision_cost_40_ip
		days_remove = 50
	
		remove_effect = {
			country_event = { id = bharat_event.4 days = 0 }
			custom_effect_tooltip = BHR_reactivate_fortification_centers
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	BHR_levy_weapon_tributes = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
			NOT = {
				has_global_flag = india_was_reunited_flag
			}
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
		
		cost = 50
		days_remove = 10
	
		remove_effect = {
			every_country = {
				limit = {
					AND = {
						is_in_faction_with = BHR
					}
					NOT = {
						tag = BHR
					}
				}
				country_event = { id = bharat_event.5 days = 1 }
			}
			custom_effect_tooltip = BHR_ask_allies_for_weapons
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	BHR_levy_supply_tributes = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
			NOT = {
				has_global_flag = india_was_reunited_flag
			}
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
		
		cost = 50
		days_remove = 10
	
		remove_effect = {
			every_country = {
				limit = {
					AND = {
						is_in_faction_with = BHR
					}
					NOT = {
						tag = BHR
					}
				}
				country_event = { id = bharat_event.8 days = 1 }
			}
			custom_effect_tooltip = BHR_ask_allies_for_medical_and_repair_kits
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	BHR_levy_united_forces = {
	
		icon = generic_industry
	
		available = {
			has_war = no
			is_puppet = no
			NOT = {
				has_global_flag = india_was_reunited_flag
			}
		}
	
		visible = {
			tag = BHR
		}
	
		fire_only_once = yes
		
		cost = 50
		days_remove = 10
	
		remove_effect = {
			every_country = {
				limit = {
					AND = {
						is_in_faction_with = BHR
					}
					NOT = {
						tag = BHR
					}
				}
				country_event = { id = bharat_event.11 days = 1 }
			}
			custom_effect_tooltip = BHR_ask_allies_for_soldiers_and_specialists
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}