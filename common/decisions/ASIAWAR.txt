asiawar_decisions = {
	CHI_balkanize_china = {
		icon = GFX_decision_political
		available = {
			hidden_trigger = {
				has_global_flag = CHI_balkanization
			}
		}
		visible = {
			#
		}
		activation = {
			country_exists = CHI
		}
		fire_only_once = yes
		days_mission_timeout = 155
		timeout_effect = {
			hidden_effect = {
				CHI_balkanization = yes
			}
			custom_effect_tooltip = CHI_balkanize_china_tooltip
		}

		ai_will_do = {
			base = 0
		}
	}
	CHI_war_warlord = {
		icon = GFX_decision_war
		state_target = yes
		on_map_mode = map_only
		fire_only_once = no
		available = {
			#
		}
		visible = {
			has_global_flag = CHI_balkanization
		}
		target_trigger = {
			FROM = {
				is_capital = yes
				OWNER = {
					is_china_tag = yes
					OR = {
						is_neighbor_of = ROOT
						ROOT = {
							any_country = {
								OR = {
									is_puppet_of = PREV
									is_in_faction_with = PREV
								}
								is_neighbor_of = FROM.OWNER
							}
						}
					}
					NOT = {
						OR = {
							is_puppet_of = ROOT
							has_war_with = ROOT
							is_in_faction_with = ROOT
						}					
					}
				}
			}
		}
		complete_effect = {
			declare_war_on = {
				target = FROM.OWNER
				type = annex_puppet_wargoal
			}
		}
		ai_will_do = {
			factor = 0
			modifier = {
				has_war = no
				add = 100
			}
		}
	}
}
