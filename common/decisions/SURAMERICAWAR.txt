suramericawar_decisions = {
	BRA_PRU_war = {
		icon = GFX_decision_unknown
		available = {
			BRA = {
				has_war_with = PRU
			}
		}
		visible = {
			#
		}
		fire_only_once = yes
		days_mission_timeout = 250
		timeout_effect = {
			custom_effect_tooltip = BRA_PRU_war_tooltip
		}
		ai_will_do = {
			factor = 1
		}
	}
	#COS_ARG_war = {  #AT WAR FROM THE START NOW!
	#	icon = GFX_decision_unknown
	#	available = {
	#		OR = {
	#			NOT = {
	#				country_exists = COS
	#			}
	#			NOT = {
	#				country_exists = ARG
	#			}
	#		}
	#	}
	#	visible = {
	#		#
	#	}
	#	fire_only_once = yes
	#	days_mission_timeout = 95
	#	timeout_effect = {
	#		COS = {
	#			declare_war_on = {
	#				target = ARG
	#				type = annex_everything
	#			}
	#		}
	#	}
	#	ai_will_do = {
	#		factor = 1
	#	}
	#}

	COS_CHL_war = {
		icon = GFX_decision_unknown
		available = {
			COS = {
				has_war_with = CHL
			}
		}
		activation = {
			NOT = {
				country_exists = ARG
			}
			country_exists = CHL
			country_exists = COS
		}
		visible = {
			#
		}
		fire_only_once = yes
		days_mission_timeout = 123
		timeout_effect = {
			COS = {
				declare_war_on = {
					target = CHL
					type = annex_everything
				}
			}
			every_state = {
				limit = {
					is_core_of = CHL
				}
				add_core_of = COS
			}
		}
		ai_will_do = {
			factor = 1
		}
	}

	COS_PRU_war = {
		icon = GFX_decision_unknown
		available = {
			OR = {
				NOT = {
					OR = {
						country_exists = PRU
						PRU = {
							is_puppet = yes
						}
					}
				}
				COS = {
					has_war_with = PRU
				}
			}
		}
		activation = {
			BGV = {
				has_war_with = PRU
			}
			country_exists = COS
			NOT = {
				country_exists = CHL
			}
		}
		visible = {
			#
		}
		fire_only_once = yes
		days_mission_timeout = 45
		timeout_effect = {
			COS = {
				declare_war_on = {
					target = PRU
					type = puppet_wargoal_focus
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
}