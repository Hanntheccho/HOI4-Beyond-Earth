scripted_gui = {	

	TNO_SG_big_event_latin_wars_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME NAME AS #IN THE TNO_SG_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "TNO_SG_big_event_latin_wars_custom_window"

		visible = {
			has_country_flag = big_event_custom_latin_wars ### this country flag triggers the #superevent
		}
	}

	TNO_SG_big_event_frame_latin_wars_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME #NAME AS IN THE TNO_SG_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "TNO_SG_big_event_frame_latin_wars_custom"

		visible = {
			has_country_flag = big_event_custom_latin_wars
		}

		effects = {
			Option_click = {
				clr_country_flag = big_event_custom_latin_wars ### this disables the country flag #that fires the superevent (so the superevent actually goes away)
			}
		}
	}

	TNO_SG_big_event_unity_building_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME NAME AS #IN THE TNO_SG_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "TNO_SG_big_event_unity_building_custom_window"

		visible = {
			has_country_flag = big_event_custom_unity_building ### this country flag triggers the #superevent
		}
	}

	TNO_SG_big_event_frame_unity_building_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME #NAME AS IN THE TNO_SG_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "TNO_SG_big_event_frame_unity_building_custom"

		visible = {
			has_country_flag = big_event_custom_unity_building
		}

		effects = {
			Option_click = {
				clr_country_flag = big_event_custom_unity_building ### this disables the country flag #that fires the superevent (so the superevent actually goes away)
			}
		}
	}

	TNO_SG_big_event_fi_fails_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME NAME AS #IN THE TNO_SG_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "TNO_SG_big_event_fi_fails_custom_window"

		visible = {
			has_country_flag = big_event_custom_fi_fails ### this country flag triggers the #superevent
		}
	}

	TNO_SG_big_event_frame_fi_fails_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME #NAME AS IN THE TNO_SG_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "TNO_SG_big_event_frame_fi_fails_custom"

		visible = {
			has_country_flag = big_event_custom_fi_fails
		}

		effects = {
			Option_click = {
				clr_country_flag = big_event_custom_fi_fails ### this disables the country flag #that fires the superevent (so the superevent actually goes away)
			}
		}
	}
}
#set_country_flag big_event_custom_latin_wars ## quick command for testing