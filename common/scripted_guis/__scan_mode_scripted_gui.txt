scripted_gui = {
	scan_mode_window_gui = {
		context_type = player_context
		
		window_name = "scan_mode_window"

        visible = {
			has_country_flag = scan_mode_on
        }

        ai_enabled = {
            always = no
        }
    } 
	scan_mode_window_gui_pol = {
		context_type = player_context
		parent_window_token = politics_tab
		
		window_name = "scan_moade_political"

        visible = {
			has_country_flag = scan_mode_on
        }
		
        ai_enabled = {
            always = no
        }
    } 
	scan_mode_window_gui_build = {
		context_type = player_context
		parent_window_token = construction_tab
		
		window_name = "scan_mode_building"

        visible = {
			has_country_flag = scan_mode_on
        }
		
        ai_enabled = {
            always = no
        }
    } 
	scan_mode_window_gui_deploy = {
		context_type = player_context
		parent_window_token = deployment_tab
		
		window_name = "scan_mode_deploy"

        visible = {
			has_country_flag = scan_mode_on
        }
		
        ai_enabled = {
            always = no
        }
    } 
    scan_mode_window_gui_state = {
		context_type = player_context
		parent_window_token = selected_state_view
		
		window_name = "scan_mode_state_view"

        visible = {
			has_country_flag = scan_mode_on
        }
		
        ai_enabled = {
            always = no
        }
    } 
	scan_mode_window_gui_iss = {
		context_type = player_context
		
		window_name = "scan_mode_international"

        visible = {
			has_country_flag = scan_mode_on
			has_country_flag = international_moment_flag
        }
		
        ai_enabled = {
            always = no
        }
    } 
	scan_mode_window_gui_south_pole = {
		context_type = player_context
		
		window_name = "scan_mode_south_pole"

        visible = {
			has_country_flag = scan_mode_on
			has_country_flag = south_pole_moment_flag
        }
		
        ai_enabled = {
            always = no
        }
    }
}
