start_indian_civil_war = {
    declare_war_on = {
        target = KAS
        type = annex_everything
    }
    declare_war_on = {
        target = PUN
        type = annex_everything
    }
    declare_war_on = {
        target = SIN
        type = annex_everything
    }
    declare_war_on = {
        target = RAJ
        type = annex_everything
    }
    declare_war_on = {
        target = KAV
        type = annex_everything
    }
    declare_war_on = {
        target = BHR
        type = annex_everything
    }
    declare_war_on = {
        target = BNG
        type = annex_everything
    }
    declare_war_on = {
        target = NAX
        type = annex_everything
    }
    declare_war_on = {
        target = MAH
        type = annex_everything
    }
    declare_war_on = {
        target = ODS
        type = annex_everything
    }
    declare_war_on = {
        target = KRT
        type = annex_everything
    }
    declare_war_on = {
        target = KRL
        type = annex_everything
    }
    declare_war_on = {
        target = TAM
        type = annex_everything
    }
}